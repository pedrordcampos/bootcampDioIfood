function meuNome (name) {
    console.log(`Meu nome é ${name}`);
}

meuNome('Pedro');


function divide(doces) {
    let result = (doces / 2);
    return result;
}

divide('10');

const divisao = divide(10);
console.log(`Temos ${divisao} kg de doces`);
